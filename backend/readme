backend/package.json
功能：Node.js项目配置和依赖管理
包含：
- 项目基本信息
- 依赖包列表（express, cors, neo4j-driver等）
- 运行脚本配置

backend/server.js - 后端服务器
功能：Express服务器主入口
包含：
- 中间件配置（CORS、静态文件服务）
- 路由注册
- 服务器启动逻辑
- 静态文件服务配置

backend/routes/knowledge-graph.js - API路由
功能：处理知识图谱相关的API请求
包含：
- Neo4j数据库连接
- Cypher查询执行
- 数据格式转换
- 错误处理

 backend/import-data.js - 数据导入脚本
核心作用： 将CSV数据导入到Neo4j图数据库

news.js - 新闻数据API路由
核心作用： 提供表格形式的新闻数据展示
获取所有新闻 GET /api/news/
功能： 查询所有主题及其关联信息，以表格形式返回

package-lock.json 文件详解
核心作用
package-lock.json 是 npm 的依赖关系锁定文件，主要作用是：
锁定确切的依赖版本
确保安装一致性
加速依赖安装过程
